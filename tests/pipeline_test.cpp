#include <gtest/gtest.h>

#include "generation/pipeline/pipeline.hpp"



TEST(PipelineTest, InitializationIsCorrect) {
    // Arrange
    // Pipeline pipe;

    // Act & Assert
    // EXPECT_TRUE(pipe.isValid());

    // Simple placeholder test to verify everything compiles
    EXPECT_EQ(1 + 1, 2);
}

